# TMDB Discover+ Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED FOR PERSISTENT STORAGE
# ===========================================

# MongoDB connection string for storing user configurations
# Without this, configs are stored in memory and lost on restart
# Get a free cluster at https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tmdb-discover-plus

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Server port (default: 7000)
PORT=7000

# Logging level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# CORS configuration (default: * allows all origins)
# For production, set to your specific domain(s), comma-separated
CORS_ORIGIN=*
# CORS_ORIGIN=https://yourdomain.com,https://app.yourdomain.com

# Enable CORS credentials (default: false)
CORS_ALLOW_CREDENTIALS=false

# ===========================================
# BEAMUP DEPLOYMENT (auto-set by Beamup)
# ===========================================

# Custom Beamup subdomain (auto-detected from deployment)
# BEAMUP_SUBDOMAIN=your-subdomain

# ===========================================
# DEBUGGING (use in development only)
# ===========================================

# Enable debug logging for TMDB API requests
# DEBUG_TMDB=1

# Disable TLS certificate verification (NOT recommended for production)
# Only use if you're behind a corporate proxy with SSL inspection
# DISABLE_TLS_VERIFY=true

# Disable rate limiting (NOT recommended for production)
# DISABLE_RATE_LIMIT=true
